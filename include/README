

# Project `include` directory

This directory contains header files for StepperController (limit switches version).
Place shared declarations and macros here for use across multiple source files.

Example usage:
```cpp
#include "MyHeader.h"
```

Header files should end with `.h` and be included where needed.
See GCC documentation for more info:
https://gcc.gnu.org/onlinedocs/cpp/Header-Files.html
